services:
  - type: web
    name: postwoman
    env: docker
    plan: free
    dockerCommand: uvicorn backend.main:app --host 0.0.0.0 --port 8000
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8000
